[supervisord]
nodaemon=true

[program:ssh]
command = /usr/sbin/sshd -D 
autostart=true
autorestart=true
redirect_stderr=true

[program:nginx]
command = nginx -g 'daemon off;'
autostart=true
autorestart=true
redirect_stderr=true